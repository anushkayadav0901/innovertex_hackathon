# Copy this file to .env and fill in values where needed.
# Vite exposes vars that start with VITE_

# Optional: GitHub token to increase rate limits for repo metadata fetches (no special scopes required)
# VITE_GITHUB_TOKEN=

# Optional: Figma personal access token for richer file metadata (not required for public oEmbed)
# VITE_FIGMA_TOKEN=
